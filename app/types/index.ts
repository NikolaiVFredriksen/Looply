export type Loop = {
  id: string;
  title: string;
  description: string;
  why: string;
};

export type ClosedLoop = {
  id: string;
  title: string;
  description: string;
  why: string;
  closedAt: string;
};
